<?xml version="1.0" encoding="utf-8"?>
<!-- SDL's android-project manifest, trimmed to what the game needs. The
     activity attributes are the ones SDLActivity documents: the
     configChanges list keeps a rotation or a keyboard from recreating
     the activity (and with it the native side), singleTask keeps one
     game per device, landscape is what the 800x600 frame is. INTERNET
     is the game server; nothing else is asked for - the data tree goes
     under the app's own external files directory, which needs no
     storage permission. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

	<uses-feature android:glEsVersion="0x00020000" android:required="true" />
	<uses-feature android:name="android.hardware.touchscreen" android:required="false" />
	<uses-feature android:name="android.hardware.gamepad" android:required="false" />

	<uses-permission android:name="android.permission.INTERNET" />

	<application
		android:label="@string/app_name"
		android:allowBackup="false"
		android:hardwareAccelerated="true"
		android:usesCleartextTraffic="true">

		<activity
			android:name=".DarkEdenActivity"
			android:label="@string/app_name"
			android:alwaysRetainTaskState="true"
			android:launchMode="singleTask"
			android:configChanges="layoutDirection|locale|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation"
			android:screenOrientation="sensorLandscape"
			android:preferMinimalPostProcessing="true"
			android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
	</application>

</manifest>
