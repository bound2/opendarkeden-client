%268
A helmet whose shell combines reinforced plastic and titanium, with every built-in function fully automated. It uses an AI chip and actively fixes the earlier flaw of failing to display accurate data on objects. Praised for overcoming every weakness in durability, shock absorption and defense, it adds an automatic zoom system to the scope. A small chip built into the helmet sends the exact target to a small lens on the left and outputs detailed data on the target. Equipped with the AAIS (Automatic-Artificial Intelligent Scanning) system.