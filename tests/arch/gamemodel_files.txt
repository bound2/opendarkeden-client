# gamemodel membership (docs/RESTRUCTURING.md task 4.1) - game-model
# code that has no SDL, UI or dxlib reach: today the pure data tables
# (experience, item options, game strings, sounds, system availability,
# fame) and the string/logging support they are built on.
#
# Read by three consumers that must never disagree about what "the
# library" is: CMakeLists.txt (the gamemodel target's source list -
# .cpp lines only), tests/arch/check_includes.pl (rules M0-M2; the .h
# lines are the headers the closure may include) and
# tests/ratchet/ratchets.sh (R4). One repository-relative path per
# line, column 1; '#' starts a comment.
#
# Extend deliberately: a file joins when its include closure passes M1
# (basic/, Client/framelib/, Client/Packet/, Client/Client_PCH.h and the
# headers listed here) and it references no executable global it does
# not define itself. The seams that had to be cut to get here are
# recorded in the plan's 4.1 status.

# Experience tables
Client/ExperienceTable.cpp
Client/ExperienceTable.h
Client/ExpInfo.cpp
Client/ExpInfo.h

# Item option table
Client/MItemOptionTable.cpp
Client/MItemOptionTable.h

# Item table (task 4.4, first slice): the per-class item definitions,
# loaded from Item.inf (the server's in-code copy was deleted, 5.2).
Client/MItemTable.cpp
Client/MItemTable.h
Client/ItemClassDef.h
Client/DrawTypeDef.h

# Game string table (the built-in English text and the String.inf loader)
Client/MGameStringTable.cpp
Client/MGameStringTable.h

# Sound table
Client/MSoundTable.cpp
Client/MSoundTable.h
Client/SoundDef.h

# System availability filters
Client/SystemAvailabilities.cpp
Client/SystemAvailabilities.h

# Fame table (SkillDef.h only for the SKILLDOMAIN enum)
Client/FameInfo.cpp
Client/FameInfo.h
Client/SkillDef.h

# Money (task 4.2; the price and trade managers wait for the item core)
Client/MMoneyManager.cpp
Client/MMoneyManager.h

# Item core (task 4.4, second slice): MItem, the gear/armour/weapon
# families and (since 4.3) the container-based gear; the use handlers
# are the executable's MItemUse.cpp. MObject is the base; MItemLimits.h
# holds the pile sizes both halves read.
Client/MItem.cpp
Client/MItem.h
Client/MItemLimits.h
Client/MObject.cpp
Client/MObject.h
# The item managers (task 4.3, first slice): the id map, the grid and the
# slot containers, the finder predicates, and the quick-slot globals the
# id map clears on release (MQuickSlot.cpp defines them).
Client/MItemManager.cpp
Client/MItemManager.h
Client/MGridItemManager.h
Client/MSlotItemManager.h
Client/MGridItemManager.cpp
Client/MSlotItemManager.cpp
Client/MItemFinder.h
Client/MQuickSlot.cpp
Client/MQuickSlot.h

# The containers (task 4.3, second slice): the inventory grid, the
# storage boxes and the shop shelves. Their reaches - the player's affect
# check and the landing sound - go through MItem's host.
Client/MInventory.cpp
Client/MInventory.h
Client/MStorage.cpp
Client/MStorage.h
Client/MShopShelf.cpp
Client/MShopShelf.h
Client/MGameDef.h

# Trade (task 4.2, second slice): the two-inventory exchange over the
# containers and the money manager, and the size-ordered item map it
# packs with; its one reach, the accept-delay clock, is installed by the
# executable.
Client/MTradeManager.cpp
Client/MTradeManager.h
Client/MSortedItemManager.cpp
Client/MSortedItemManager.h

# Prices (task 4.2, third slice): what a shop charges or pays for an
# item, over the item and option tables, the timed-item register and the
# user's head-price rate; the player's race, level and stats, the
# half-price event and skills, and the shop tax come through MPriceHost,
# installed by the executable.
Client/MPriceManager.cpp
Client/MPriceManager.h

# The gear (task 4.4, third slice): the slot container a player wears
# its items in, the three races' slot rules over it, and the shop that
# holds a shelf per type. Their reaches - the player's affect check and
# stat recalculation, the gear sound, the quick-slot rebuild, the repair
# hint and the empty magazine a gun is given - go through MItem's host.
Client/MPlayerGear.cpp
Client/MPlayerGear.h
Client/MSlayerGear.cpp
Client/MSlayerGear.h
Client/MVampireGear.cpp
Client/MVampireGear.h
Client/MOustersGear.cpp
Client/MOustersGear.h
Client/MShop.cpp
Client/MShop.h

# The skill core (task 4.4, fourth slice): the skill info table, the set
# of skills a character may use, and the domains and the tree over them.
# The half that asks what the player can use right now is the
# executable's MSkillAvailable.cpp; the delays run on the item host's
# clock. SkillDef.h, the enumerations, is listed with the fame table
# above, which already needed its SKILLDOMAIN enum.
Client/MSkillManager.cpp
Client/MSkillManager.h
Client/MSkillInfoTable.cpp
Client/SkillDef.cpp

# The seams MItem reads (task 4.4): the user's display settings (the
# gore level picks the skull names), the client configuration (the
# colour-set bases) and the timed-item register (quest items). All
# three are pure loaders over their own state.
Client/UserInformation.cpp
Client/UserInformation.h
Client/ClientConfig.cpp
Client/ClientConfig.h
Client/MusicDef.h
Client/MTimeItemManager.cpp
Client/MTimeItemManager.h

# Support: the string class, the typed table template and the logger
Client/MString.cpp
Client/MString.h
Client/MStringArray.cpp
Client/MStringArray.h
Client/CTypeTable.h
Client/MTypeDef.h
