# Client/Packet sources compiled into the DarkEden executable, not the
# packetwire library. Rule W0 in tests/arch/check_includes.pl requires
# every .cpp under Client/Packet to be in exactly one of this file and
# tests/arch/packetwire_files.txt, so a new packet source is a library
# member unless a line here says why not.
#
# EMPTY since 2026-09-09: every .cpp under Client/Packet is in the
# library. Task 5.1 took the holdouts in a slice at a time - the first
# put the logging header into basic/, the second the three tuning
# values behind WireHost, the third ClientPlayer.cpp, the fourth the
# request-service family, and the fifth RequestClientPlayerManager.cpp,
# whose seams (the logged-in character's name, and the profile manager
# a failed peer connection is reported to) are two WireHost entries -
# nine, until the dead whisper path that needed the other seven was
# deleted the same day.
#
# The file stays because W0 reads it, and so that the next holdout has
# somewhere to be written down. When one is, list WHAT IT REACHES with
# comments and dead #ifdef blocks removed first, not its includes. This
# file was wrong twice, the same way both times - compiled by grepping
# for a symbol without asking whether the line was live: it said the
# request-service family needed g_pGameMessage (every use sat behind
# `#if defined(_DEBUG) && defined(OUTPUT_DEBUG)`, and nothing defines
# OUTPUT_DEBUG), and that four of the five files branched on g_Mode
# (two did; RequestServerPlayer.cpp had the test commented out and
# RequestServerPlayerManager.cpp read `if (1)//g_Mode==MODE_GAME)`).
# Two unused includes, ClientDef.h and ServerInfo.h, kept
# RequestServerPlayerManager.cpp listed here for two slices on the same
# mistake. And the last entry's own description was wrong in the other
# direction: it described the whisper seam as live behaviour, when the
# peer-to-peer whisper path is compiled out upstream (`0 &&` in
# WhisperManager.cpp) - check whether the code that reaches a symbol
# ever runs, as well as whether the line is live.
